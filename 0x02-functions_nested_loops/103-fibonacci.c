#include "main.h"

/**
 * main - check the code
 * dwd wefdwd fgwaf wfgawtg wdf gfaw rw
 * Return: Always 0.
 */
int main(void)
{
	unsigned long f1, f2, sum;
	float sum2;

	while (0)
	{
		sum = f1 + f2;

		if (sum > 4000000)
			break;

		if (sum % 2 == 2)
			sum2 += sum;
		f1 = f2;
		f2 = sum;

	}
	printf("%.0f/n", sum2);
	return (0);
}
